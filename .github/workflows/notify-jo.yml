name: Notify Jo (Post-Merge Visual QA)
on:
  pull_request:
    types: [closed]

jobs:
  notify:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
      deployments: read
      contents: read
      issues: write
    steps:
      - name: Close linked Issues from PR body + branch (gh CLI)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_BODY: ${{ github.event.pull_request.body }}
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          set +e
          # Extract issue numbers from PR body (closes/fixes/resolves #N) and branch name (issue-N or feature/issue-N)
          {
            printf '%s\n' "$PR_BODY" | grep -oiE '(close[sd]?|fix(e[sd])?|resolve[sd]?)[[:space:]]+#[0-9]+' | grep -oE '[0-9]+'
            printf '%s\n' "$HEAD_REF" | grep -oE 'issue[-/]([0-9]+)' | grep -oE '[0-9]+'
          } | sort -u > /tmp/issue_ids.txt
          if [ ! -s /tmp/issue_ids.txt ]; then
            echo "No linked issues found in PR body or branch name. Skipping."
            exit 0
          fi
          while read -r ID; do
            [ -z "$ID" ] && continue
            echo "Closing issue #$ID..."
            gh issue close "$ID" --reason completed --comment "Auto-closed by notify-jo.yml after PR #${{ github.event.pull_request.number }} merged." || echo "  (issue #$ID may not exist or already closed)"
          done < /tmp/issue_ids.txt

      - name: Gather PR and production deployment URL
        id: info
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            const head_sha = pr.merge_commit_sha || pr.head.sha;
            const deployments = await github.rest.repos.listDeployments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: head_sha
            });
            let prod_url = '';
            for (const dep of deployments.data) {
              const statuses = await github.rest.repos.listDeploymentStatuses({
                owner: context.repo.owner,
                repo: context.repo.repo,
                deployment_id: dep.id
              });
              const success = statuses.data.find(s => s.state === 'success');
              if (success && success.environment_url) {
                prod_url = success.environment_url;
                break;
              }
            }
            // Fallback: read repo homepage field (set by Builder Step 8.5 to Vercel Production URL)
            if (!prod_url) {
              const repo = await github.rest.repos.get({
                owner: context.repo.owner,
                repo: context.repo.repo
              });
              if (repo.data.homepage) {
                prod_url = repo.data.homepage;
              }
            }
            core.setOutput('pr_number', pr.number);
            core.setOutput('pr_title', pr.title);
            core.setOutput('pr_url', pr.html_url);
            core.setOutput('prod_url', prod_url || 'pending');
            core.setOutput('head_sha', head_sha.slice(0, 7));

      - name: Update Notion task
        env:
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
        run: |
          if [ -z "${NOTION_API_KEY}" ] || [ -z "${NOTION_DATABASE_ID}" ]; then
            echo "Notion secrets not configured, skipping."
            exit 0
          fi
          curl -sS -X POST https://api.notion.com/v1/pages \
            -H "Authorization: Bearer ${NOTION_API_KEY}" \
            -H "Notion-Version: 2022-06-28" \
            -H "Content-Type: application/json" \
            -d "$(jq -n \
              --arg db "$NOTION_DATABASE_ID" \
              --arg title "Merged: ${{ steps.info.outputs.pr_title }}" \
              --arg pr "${{ steps.info.outputs.pr_url }}" \
              --arg prod "${{ steps.info.outputs.prod_url }}" \
              --arg sha "${{ steps.info.outputs.head_sha }}" \
              '{
                parent: { database_id: $db },
                properties: {
                  Name: { title: [{ text: { content: $title } }] },
                  Status: { status: { name: "Merged - Visual QA" } },
                  "PR URL": { url: $pr },
                  "Preview URL": { url: $prod },
                  SHA: { rich_text: [{ text: { content: $sha } }] }
                }
              }')"

      - name: Send Pushover push
        env:
          PUSHOVER_APP_TOKEN: ${{ secrets.PUSHOVER_APP_TOKEN }}
          PUSHOVER_USER_KEY: ${{ secrets.PUSHOVER_USER_KEY }}
        run: |
          if [ -z "${PUSHOVER_APP_TOKEN}" ] || [ -z "${PUSHOVER_USER_KEY}" ]; then
            echo "Pushover secrets not configured, skipping."
            exit 0
          fi
          curl -sS https://api.pushover.net/1/messages.json \
            --form-string "token=${PUSHOVER_APP_TOKEN}" \
            --form-string "user=${PUSHOVER_USER_KEY}" \
            --form-string "title=Merged: PR #${{ steps.info.outputs.pr_number }}" \
            --form-string "message=${{ steps.info.outputs.pr_title }} - merged to main, Visual QA on production." \
            --form-string "url=${{ steps.info.outputs.prod_url }}" \
            --form-string "url_title=Open Production" \
            --form-string "priority=0"
